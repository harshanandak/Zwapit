---
import AppShell from "../../../components/AppShell.astro";
import AuthActionGate from "../../../components/AuthActionGate.astro";
import { getAuthActionState } from "../../../lib/auth/authAdapter";

const uploadAction = getAuthActionState("sell", "/app/sell/upload", undefined, { requirePhoneVerified: true });
const ordersAction = getAuthActionState("sell", "/app/sell/orders", undefined, { requirePhoneVerified: true });
---

<AppShell routeId="/app/sell" title="Sell" activeTab="sell">
  <div class="space-y-5">
    <div>
      <h2 class="text-lg font-semibold text-foreground">Sell a ticket you can't use</h2>
      <p class="mt-1 text-sm text-muted-foreground">
        Upload it first — we detect the details, you set a price, and buyers pay with protection.
      </p>
    </div>

    <AuthActionGate
      state={uploadAction}
      label="Upload to Sell"
      class="block rounded-xl bg-primary px-4 py-3 text-center text-sm font-semibold text-primary-foreground shadow-sm"
    />

    <section class="rounded-xl border border-border bg-card p-4">
      <h3 class="text-sm font-semibold text-foreground">How selling works</h3>
      <ol class="mt-3 space-y-2 text-sm text-muted-foreground">
        <li>1. Upload your ticket or pass.</li>
        <li>2. We detect the details — no long forms.</li>
        <li>3. Set your price and see your payout.</li>
        <li>4. Make your seller promise and go live.</li>
      </ol>
    </section>

    <a
      href={ordersAction.status === "allowed" ? ordersAction.href : `/app/me?next=${encodeURIComponent(ordersAction.href)}`}
      class="flex items-center justify-between rounded-xl border border-border bg-card px-4 py-3 text-sm"
      data-auth-action={ordersAction.action}
      data-auth-state={ordersAction.status}
    >
      <span>
        <span class="font-semibold text-foreground">{ordersAction.status === "allowed" ? "Orders" : "Sign in to continue"}</span>
        <span class="block text-xs text-muted-foreground">
          {ordersAction.status === "allowed" ? "Tickets people have bought from you" : "Open your account to continue"}
        </span>
      </span>
      <span aria-hidden="true" class="text-muted-foreground">›</span>
    </a>
  </div>
</AppShell>
